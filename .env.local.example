# Google AI API key used by @ai-sdk/google in /api/generate.
GOOGLE_GENERATIVE_AI_API_KEY=

# Vercel Blob (optional). When set, uploads and generated images go to Blob instead of local public/.
# Required for production on Vercel. Create a Blob store in the dashboard, then: vercel env pull
# BLOB_READ_WRITE_TOKEN=
